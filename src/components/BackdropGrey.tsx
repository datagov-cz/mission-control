import { Box } from '@material-ui/core'
import { styled } from '@material-ui/core/styles'

const BackdropGrey = styled(Box)({
  background: '#eee',
  minHeight: '100vh',
  display: 'flex',
  flexDirection: 'column',
})

export default BackdropGrey
