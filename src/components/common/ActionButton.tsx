import { Button, styled } from "@mui/material";

export const ActionButton = styled(Button)(() => ({
  color: "black",
  backgroundColor: "white",
  "&:hover": {
    color: "black",
    backgroundColor: "white",
  },
}));
