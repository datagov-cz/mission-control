import { PREFIX } from 'utils/vocabulary'

export const ID_CONTEXT = {
  id: '@id',
  types: '@type',
  firstName: `${PREFIX}má-křestní-jméno`,
  lastName: `${PREFIX}má-příjmení`,
  username: `${PREFIX}má-uživatelské-jméno`,
}
